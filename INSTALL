Sitix uses CMake. To install it, you'll need to CMake this project into a new build directory then cmake --build.
On Linux, that would look like:
mkdir build
cd build
cmake ..
cmake --build .

That will create a sitix binary that you can copy to a PATH-accessible location or something.